version: '3.8'

services:
  clipforge:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    volumes:
      - temp_data:/app/server/temp
    restart: unless-stopped

volumes:
  temp_data:
